export default defineAppConfig({
  header: {
    // Title to display if no logo
    title: "Inspira UI",
    // Logo configuration
    logo: {
      alt: "Inspira UI Logo",
      // Light mode
      light: "",
      // Dark mode
      dark: "",
    },
  },
});
